///
/// HybridLanguageModelSessionSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeGenerationOptions` to properly resolve imports.
namespace margelo::nitro::rnfoundationmodels { struct NativeGenerationOptions; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "NativeGenerationOptions.hpp"
#include <optional>
#include <functional>

namespace margelo::nitro::rnfoundationmodels {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `LanguageModelSession`
   * Inherit this class to create instances of `HybridLanguageModelSessionSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridLanguageModelSession: public HybridLanguageModelSessionSpec {
   * public:
   *   HybridLanguageModelSession(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridLanguageModelSessionSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridLanguageModelSessionSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridLanguageModelSessionSpec() override = default;

    public:
      // Properties
      virtual bool getWasContextReset() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> respond(const std::string& prompt, const std::optional<NativeGenerationOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::string>> streamResponse(const std::string& prompt, const std::function<void(const std::string& /* stream */)>& onStream, const std::optional<NativeGenerationOptions>& options) = 0;
      virtual std::shared_ptr<Promise<double>> tokenCount(const std::string& prompt) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "LanguageModelSession";
  };

} // namespace margelo::nitro::rnfoundationmodels
